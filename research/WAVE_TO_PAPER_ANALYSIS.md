# ZONES Wave-To-Paper Analysis

## Wave 1: Foundation Contract

Status: active.

Research output:

- Supports B.0 `temporal-zone-model`.
- Does not yet support measurement claims.

Evidence produced:

- Rust seed evaluator.
- RPLAN-backed context ingestion path.
- Temporal/global model structs and validation tests.
- Specs, roles, and research backlog.

Paper readiness:

- B.0 can accumulate methods notes.
- No formal paper should be written yet.

## Wave 2: Source Inventory

Expected research output:

- B.1 `boundary-source-audit`.
- D.2 `reform-pathways` source section.
- B.2 `global-time-rules` source section.

Evidence required:

- DOT/NIST/Congress/Federal Register legal notes.
- Census/TIGER/GEOID boundary notes.
- Legal time-zone boundary source audit.
- IANA/national-source audit.

Paper readiness:

- Source-audit working paper can begin after role review.

## Wave 3: US County Baseline

Expected research output:

- A.0 `solar-noon-baseline`.
- A.1 `county-deviation-atlas`.
- A.2 `dst-shift-analysis`.

Evidence required:

- County graph and current legal timezone assignments.
- Population-weighted solar-noon error.
- Standard-time and DST-period scorecards.
- Uncertainty notes for internal point versus population center.

Paper readiness:

- Measurement papers can start after scorecard reproducibility.

## Wave 4: Temporal Global Model

Expected research output:

- B.0 `temporal-zone-model`.
- E.2 `country-boundary-pilots`.

Evidence required:

- Non-US pilot.
- Time-versioned regime fixture.
- Source limits and legal caveats.

Paper readiness:

- Temporal model paper can be drafted after one non-US pilot passes.

## Wave 5: Candidate Search

Expected research output:

- C.0 `timezone-redistricting`.
- C.1 `minimum-change-zones`.
- C.2 `state-county-tradeoffs`.

Evidence required:

- Candidate families.
- Reproducible seeds or deterministic search.
- Boundary cut, contiguity, move-count, and solar-error deltas.

Paper readiness:

- Optimization papers only after current-law baseline is stable.

## Wave 6: Publication Packets

Expected research output:

- D.0 `convenience-of-commerce`.
- D.1 `public-disruption-metrics`.
- Policy briefs only after role review.

Evidence required:

- DOT-convenience proxy layer.
- Public-map role review.
- Explicit recommendation gate.

Paper readiness:

- Reform arguments belong here, not before.
